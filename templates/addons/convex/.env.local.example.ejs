# Convex Configuration
# Get these values by running: npx convex dev
#
# 1. Run `npx convex dev` to start local development
# 2. Copy your deployment URL below
# 3. Rename this file to .env.local

CONVEX_URL=https://your-deployment.convex.cloud
<% if (archetype === 'full-stack' || webFramework) { -%>

# For client-side usage (prefix with VITE_ for Vite, NEXT_PUBLIC_ for Next.js)
<% if (webFramework === 'nextjs') { -%>
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud
<% } else { -%>
VITE_CONVEX_URL=https://your-deployment.convex.cloud
<% } -%>
<% } -%>
