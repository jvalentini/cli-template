import templateConfig from '../template.config'

export default {
  siteTitle: templateConfig.brandName,
  siteDescription: templateConfig.projectDescription,
  siteUrl: templateConfig.siteUrl,
  siteImage: '/images/og-image.png',
  favicon: '/favicon.ico',
  twitterHandle: templateConfig.twitterHandle,
  email: templateConfig.supportEmail,
  address: templateConfig.companyAddress,
}
